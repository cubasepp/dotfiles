# SSH Key Issue Remove SSH_AUTH_SOCK and set it manually
set -g update-environment "DISPLAY SSH_ASKPASS SSH_AGENT_PID SSH_CONNECTION WINDOWID XAUTHORITY"
setenv -g SSH_AUTH_SOCK $HOME/.ssh/auth_socket.sock
