# 3-pane dev layout: large main pane on the left, two stacked panes on the right.
# Sourced by `prefix + D` (see tmux.conf). New panes open in the current pane's path.
split-window -h -l 35% -c "#{pane_current_path}"
split-window -v -c "#{pane_current_path}"
select-pane -L
